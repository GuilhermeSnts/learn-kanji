import { styled } from "@/styles/theme";

export const StyledLevelSelect = styled("section", {
  color: "$text",
  marginBottom: "$md",
  marginTop: "$lg",
  h2: {
    marginBottom: "$md",
    fontSize: "$lg",
    fontWeight: 'normal',
  },
});
